import { Card } from "@/components/ui/card";

export default function AdminOrdersPage() {
  return <Card className="p-8 text-black">Order pipeline with payment state, fulfillment state, customer contact, and export actions.</Card>;
}
