import { Card } from "@/components/ui/card";

export default function AdminInventoryPage() {
  return <Card className="p-8 text-black">Inventory thresholds, low-stock alerts, and restock planning view.</Card>;
}
