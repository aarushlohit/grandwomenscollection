import { Card } from "@/components/ui/card";

export default function AdminAIPage() {
  return <Card className="p-8 text-black">AI provider routing, prompt testing, recommendation tuning, and fallback visibility.</Card>;
}
