import { Card } from "@/components/ui/card";

export default function AdminSettingsPage() {
  return <Card className="p-8 text-black">Store settings, SMTP status, Telegram connectivity, tax/shipping defaults, and key health.</Card>;
}
