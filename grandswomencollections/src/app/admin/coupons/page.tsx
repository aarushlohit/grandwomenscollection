import { Card } from "@/components/ui/card";

export default function AdminCouponsPage() {
  return <Card className="p-8 text-black">Coupon CRUD, validation windows, spend thresholds, and campaign attribution.</Card>;
}
